# Surgical Image Edit Template v3.2

Edit the existing approved image only.

CHANGE:
- List only the explicit requested delta(s).

PRESERVE EXACTLY:
- Every approved subject/identity/pose lock
- Composition and camera unless explicitly changed
- Lighting/color unless explicitly changed
- Approved environment elements unless explicitly changed
- Water/physics/material behavior unless explicitly changed
- Aspect/framing unless explicitly changed

Do not reroll or reinterpret unaffected parts of the image.
